/** ---------------------------------------------------------------------------------------------------------------------
 * @file environment.ts — development. `baseUrl` must include the backend's global `api/v1` prefix.
 */
export const environment = {
  production: false,
  baseUrl: 'http://localhost:3000/api/v1',
  resourceUrl: 'http://localhost:3000',
};
