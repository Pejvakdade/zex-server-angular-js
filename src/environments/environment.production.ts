/** ---------------------------------------------------------------------------------------------------------------------
 * @file environment.ts — production. `baseUrl` must include the backend's global `api/v1` prefix.
 */
export const environment = {
  production: true,
  baseUrl: 'https://api.zexserver.com/api/v1',
  resourceUrl: 'https://api.zexserver.com',
};
